#include <iostream>
#include <string>
using namespace std;
int main()
{
int i,f=1,j;
string s;
cin>>s;
j=(s.length())-1;
for(i=0;i<(s.length())/2;i++)
{
if(s[i]!=s[j])
{f=2;break;}
j--;
}
if(f==2)
cout<<"NO"<<endl;
else
cout<<"YES"<<endl;
return 0;
}
